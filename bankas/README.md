# Skafis bankas automatisation

`/firestore-management` for uploading and backuping data.

`/pdf-and-md-parsing` for extracting data form PDFs and Markdown files.

`/other` for misc.

Both have their own READMEs.
